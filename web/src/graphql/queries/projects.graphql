query Projects {
  projects {
    id
    display
    title
    shortName
    difficulty
    cover
    redirect
  }
}

query AllProjects {
  allProjects {
    id
    display
    title
    shortName
    difficulty
    cover
    redirect
  }
}

query ProjectByShortName($shortName: String!) {
  projectByShortName(shortName: $shortName) {
    id
    display
    title
    shortName
    difficulty
    description
    cover
    redirect
  }
}
