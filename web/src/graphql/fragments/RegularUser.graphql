fragment RegularUser on User {
  id
  uniId
  firstName
  lastName
  email
  role
  memberFromDate
  isMember
  projects {
    id
    shortName
  }
  talks {
    id
    shortName
  }
  courses {
    id
    shortName
  }
  tutorials {
    id
    shortName
  }
}

fragment RegularUserWithoutEvents on User {
  id
  uniId
  firstName
  lastName
  email
  role
  memberFromDate
  isMember
}